<!DOCTYPE html>
<html>
<head>
  <title>Bbq</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <script src="https://cdn.jsdelivr.net/npm/bs5-lightbox@1.8.3/dist/index.bundle.min.js"></script>

<!--  <script src="https://api-maps.yandex.ru/2.1/?apikey=<%#= ENV["API_YANDEX_MAP"] %>&lang=ru_RU" type="text/javascript">-->
  </script>
<!--  <script src="maps.js" type="text/javascript"></script>-->
  <meta name="turbo-visit-control" content="reload">
</head>

<body>
<div class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "BBQ!", root_path, class: "navbar-brand" %>
    <%#= link_to t("app.name"), root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" data-toggle="collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item"><%= link_to t(".user"), current_user, class: "nav-link" %></li>
          <li class="nav-item"><%= button_to t(".sign_out"), destroy_user_session_path, method: :delete,
                                             class: "nav-link btn btn-outline-secondary btn-sm" %></li>
        <% else %>
          <li class="nav-item"><%= link_to t(".sign_in"), new_user_session_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to t(".sign_up"),
                                           new_user_registration_path, class: "nav-link" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-lg-9">
      <% flash.each do |type, msg| %>
        <div class="mt-3 alert alert-info text-center">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
    <div class="col-lg-3">
    </div>
  </div>
  <footer>
    <p>&copy; <%= t("app.company") %> 2022</p>
  </footer>
</div>
</body>
</html>
