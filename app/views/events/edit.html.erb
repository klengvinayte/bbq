<h1 class="mt-3">Редактирование события</h1>

<div class="row">
  <div class="col-md-6 buffer-top">
    <%= render 'form' %>
  </div>

  <div class="my-3">

    <%= button_to "Удалить событие", event_path(@event), method: :delete, title: 'Удалить событие', data: { turbo_method: :delete, turbo_confirm: t('alert.notice') }, class: 'btn btn-sm btn-warning' %>
  </div>
</div>
