<h1 class="mt-3"><%= t(".title") %></h1>

<%= form_tag("", method: "post") do |f| %>
  <div class="form-group">
    <%= label "pincode", :pincode, class: "mb-1" %>
    <%= text_field_tag :pincode, params[:pincode], class: "form-control mb-3",
                       placeholder: t("activerecord.attributes.event.pincode") %>
  </div>

  <div class="actions mb-3">
    <%= submit_tag t(".save"), class: "btn btn-sm btn-primary" %>
  </div>
<% end %>

<%= link_to t(".back"), events_path %>
